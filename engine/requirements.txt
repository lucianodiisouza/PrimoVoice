# Núcleo do engine PrimoVoice (Apple Silicon / MPS)
numpy>=1.24

# Enhancement da voz
deepfilternet>=0.5.6

# Separação de stems
demucs>=4.0.1

# Torch (Apple Silicon usa MPS nativamente nas wheels oficiais)
# Fixado em 2.2.2: versões mais novas do torchaudio removeram o backend de I/O
# que o DeepFilterNet 0.5.6 ainda importa (torchaudio.backend.common).
torch==2.2.2
torchaudio==2.2.2

# De-reverb (WPE). Python puro, sem GPU. Aplicado ANTES do enhance pra voz
# limpa chegar ao isolador sem cor de sala.
nara_wpe>=0.0.3

# Backend opcional de qualidade máxima (instale com: pip install -e .[resemble])
# resemble-enhance
